#include "scoda/scoda.hpp"

using namespace std;

int main(int argc, const char** argv) {
    // Accept edge stream from stdin

    // Initialize datastructures and invoke scoda

    // Send partition data to stdout

    // TODO: scoda: produce other output? comm_graph output?

    cout << "In scoda_exec.cpp" << endl;
    return EXIT_SUCCESS;
}
